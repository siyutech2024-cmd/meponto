# 模式二上线 · 完整执行方案(总案)

> 2026-08-06 定稿。三份文档的执行汇总层:
> - 业务定稿:《模式二PRO池最终方案v3》(mode2-pro-pool-final-v3.md)——规则唯一依据
> - 系统对照:《全系统升级闭环矩阵》(mode2-upgrade-closure-matrix.md)——逐系统状态
> - APP 配套:《骑手APP整体更新方案 v2》(rider-app-update-plan-v2.md)
> 本文只回答:**按什么顺序、谁、做什么、怎么验收**。

---

## 0. 定稿架构一页图

```
┌─ 一台 VPS(圣保罗)──────────────────────────────┐
│ /opt/eastwind-scraper      旧OL账号 → 主token  │──┐
│ /opt/eastwind-scraper-pro  新OL账号 → PRO token │──┤ 5min/次,错峰2-3min
└────────────────────────────────────────────────┘  │ 纯出站,无入站端口
                                                    ▼
        POST /api/eastwind/rider-status(token→source=main/pro,批次隔离)
                                                    ▼
┌─ Vercel(PontoSys)─────────────────────────────────────────────┐
│ 实时:riders-live 双源并集+pool 筛选 · rider/live-count(骑手自查) │
│ 排班:slots 按池过滤(MODE2_POOL)· PRO 班表校验(T4)             │
│ 导入:T+1 双报表(旧OL+新OL,PRO 零金额,幂等=账号+日期+骑手)(T2)│
│ 对账:分池 + 单量×HqProRate 月结(T3) · splash audience=pro     │
└────────────────────────────────────────────────────────────────┘
                                                    ▼
┌─ Supabase ─┐  ┌─ 三级后台(零新菜单,筛选/徽章融入现有页)─┐  ┌─ 骑手端 ─┐
│ 快照双源     │  │ 总部:标记/建PRO班/锁班/对账             │  │ APP v2.6  │
│ pool 字段    │  │ 加盟商:PRO应收=单量×12 · 填PRO表        │  │ PWA 同步  │
│ 审计         │  │ 站点:PRO班表提交 · 单量协核             │  │ 老版兼容  │
└─────────────┘  └──────────────────────────────────────────┘  └───────────┘
```

核心原则(全程不变):骑手端零金额(源头导入即零)· API 只增不改 · 全部 flag 可回退 · PontoSys 零新增菜单。

## 1. 今天(D0)——地基落位

| # | 动作 | 谁 | 验收 |
|---|---|---|---|
| D0-1 | 双击 push-settlement-decouple.command | 你 | 部署后骑手账本显示 7 月至今结算;预付款负余额清零 |
| D0-2 | 双击 push-mode2-phase1.command(pool 体系+双源隔离+DDL+方案文档入库) | 你 | 推送成功;DDL 执行(source 列) |
| D0-3 | openssl rand -hex 24 → Vercel 加 EASTWIND_INGEST_TOKEN_PRO → Redeploy | 你 | 环境变量生效 |
| D0-4 | PRO 实例 .env 填 PRO token(严禁复用主 token)+ 抓取周期加 2–3 分钟偏移 → pm2 start | 你 | pm2 两实例 online |
| D0-5 | 验证:快照表出现 source=pro 批次;两源各自推进不互删 | 我 | riders-live 并集正常 |
| D0-6 | 核对 VPS 规格(两个 Chromium 建议 ≥2vCPU/4GB;pm2 max_memory_restart 800M/实例) | 你 | 内存余量 >30% |

## 2. 本周(D1–D4)——P0 开发(对应 v3.0 九任务)

| 天 | 开发内容 | 对应 |
|---|---|---|
| D1 | **T2 双源导入**:导入端点识别新 OL 报表(99ID 恒定,同 CPF 绑定)· PRO 行零金额落库 · 幂等键=账号+日期+骑手 · 缺源告警 · 同日双源同 CPF 兜底告警 · 单量对账表 | T2 |
| D1 | **T1 批量标记**:PRO 名单导入(建档缺档的+标记 pool,逐条审计)· 转池生效日=新 OL 首次导入日 | T1 |
| D2 | **三级后台 UI(零新菜单)**:骑手管理 pool 筛选(全部/PRO/普通)+徽章+仅HQ可编辑 · 排班三页 PRO 标记与两套名额校验+非 PRO 拦截 · 实时看板 PRO chip/徽章/双源新鲜度 · splash audience 控件 · **PWA 钱包页 PRO 分支(N1)** | T1/T4/T6 |
| D3 | **T3 对账分池**:HqProRate 月度配置(改价审计)· 总部/加盟商两端 PRO 小计=单量×12 · 月结支付确认留痕(不碰预付款账本) | T3 |
| D3–4 | **T7 = v2.6 开发**(上线周范围):PRO 徽章三处 · 入池欢迎页(联动 splash 去重)· 首页双口径单量卡(live-count+昨日确认)+工资核对提示(葡语)· 三语 | T7 |
| D4 | **T5 锁班链路**(依赖 99 前 2 项答复):18:00 cron+手动锁 · 名册二次池校验(H4)· 上报状态条/失败告警/留档 · 变更增量上报 | T5 |
| D4 | **T8/T9**:积分四链路各验一例(PRO 完单→积分/邀请/扫码/等级)· 全链路演练+普通池六项回归+preflight:full | T8/T9 |

> A4/A5(活动卡/WebView)明确移出上线周 → v2.7(守冻结,N3)。
> Realme 活动:splash 引流 + H5(服务端内容,不占冻结名单)。

## 3. 上线周(照 v3.0 §四执行)

| 时点 | 动作 |
|---|---|
| 前一天上午 | 总部:导入 PRO 名单标记(=新 OL 首次导入同批)· 建 PRO 排班表 · 分两套名额 |
| 前一天下午 | 加盟商:填 PRO 表(一句话新规:锁定后不能随便改,没填进表的 PRO 明天接不了单) |
| 前一天傍晚 | 总部:审核 → 18:00 锁定 → 名册报 99 → 双方核对 |
| 上线日 | MODE2_POOL=on · splash audience=pro 开 · 监控盯首时段("应岗未上"红标)· 客服"PRO 工资纠纷"分类就位 |

回退:MODE2_POOL=off(排班过滤即刻消失)+ splash 关 + 99 侧解除名册管控(待99#7)。双抓取实例照跑不受影响。

## 4. 待外部输入(不阻塞今天和 D1–D3)

| 来源 | 事项 | 阻塞 |
|---|---|---|
| 99 | 名册执行机制/粒度(前2项) | T5 |
| 99 | 新 OL 报表格式+AR 字段有无+取数时点 | T2 演练(开发可先按旧格式做) |
| 99 | 回退时解除管控路径 · 名单更新机制 · 结算价 · 对接人 | 演练/毛利 |
| 内部 | N1–N5 拍板(锁定 18:00 等,均有默认) | 上线周 |
| 内部 | iOS 取舍(本期排除 or 同步) | v2.6 发布形态 |
| 法务 | 薪酬不透明+锁班的雇佣关系确认(M3) | 上线决策 |

## 5. 总验收(上线判定,全过才开闸)

- [ ] 双源实时:两 source 各自推进,PRO 视图(?pool=pro)人数=名单数
- [ ] 导入:新 OL 报表进库,PRO 行零金额;缺源告警触发过一次演练
- [ ] 排班:PRO 骑手 APP 只见 PRO 表;普通骑手六项回归数字与上线前一致
- [ ] 对账:总部/加盟商两端 PRO 小计一致(单量×12)
- [ ] 积分:PRO 完单次日积分到账一例;邀请/扫码/等级各一例
- [ ] APP:PRO 账号见徽章+双口径卡+无任何金额;老版本 PRO 无 R$0,00 恐慌面
- [ ] 审计:入池/出池/锁班/改价全部留痕可查
- [ ] 回退演练:关 flag 后 5 分钟内全部恢复普通形态
